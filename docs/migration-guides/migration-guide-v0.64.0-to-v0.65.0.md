# [v0.65.0]

## BREAKING CHANGE

Following changes in AccelByte Gaming Services OpenAPI specification:

- `basic`: Operation `GetConfig1` (`get_config_1`) has been renamed into `GetConfig` (`get_config`).
- `basic`: Operation `UpdateConfig1` (`update_config_1`) has been renamed into `UpdateConfig` (`update_config`).

- `legal`: Operation `DownloadExportedAgreementsInCSV` parameter `policy_version_id` has been replaced by `export_id`.

[v0.65.0]: https://github.com/AccelByte/accelbyte-python-sdk/compare/v0.64.0..v0.65.0
